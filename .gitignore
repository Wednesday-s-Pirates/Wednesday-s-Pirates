# === Javaのビルド成果物を除外 ===
*.class       # Javaコンパイルで生成されるバイトコード（Main.classなど）
*.jar         # Javaアーカイブファイル（成果物）
*.war         # Webアプリケーション成果物
*.ear         # Java EE用アーカイブ

# === IDE（Eclipse）関係の除外 ===
*.log         # ログファイル（開発中に出力される）
bin/          # Eclipseのバイナリフォルダ（ビルド成果物）
.settings/    # Eclipseの設定フォルダ
.project      # Eclipseのプロジェクトファイル
.classpath    # Eclipseのビルドパス設定ファイル

# === OS固有ファイル ===
.DS_Store     # macOSのフォルダ設定ファイル（Macユーザーがいる場合）
Thumbs.db     # Windowsの画像キャッシュ（Winユーザーがいる場合）

# === その他（不要なファイルやディレクトリ）===
*.iml         # IntelliJ IDEA用ファイル（将来IDE切り替えた時用）
.idea/        # IntelliJのプロジェクト設定

# === 実行ファイルや環境依存のファイル ===
*.exe         # 実行ファイル（誤って上げないように）
*.out         # 出力ファイル
*.tmp         # 一時ファイル

# === buildフォルダ ===
build/        # GradleやMaven使用時のビルド成果物（未使用でも保険）
target/       # MavenやSpring Boot系のビルド先

# === セキュリティ・機密情報ファイル（追加）===
*.key         # 秘密鍵
*.pem         # 証明書関連
*.p12         # 証明書ファイル
*.crt         # サーバー証明書
*.env         # 環境変数ファイル（APIキーなど）
*.token       # アクセストークンやセッションキー
config/secret.properties # 任意の機密設定ファイル

# === 編集エディタなどの一時ファイル ===
*~            # 一時ファイル（古いエディタなど）
*.swp         # Vimのスワップファイル
*.swo         # Vimの一時バックアップファイル
*.bak         # バックアップファイル
*.old         # 古いファイル

# === VSCode（念のため）===
.vscode/
